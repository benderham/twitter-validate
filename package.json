{
  "name": "twitter-validate",
  "version": "1.0.5",
  "description": "A Twitter handle validation tool. Checks whether a provided handle meets Twitter's handle criteria. Also provides a method to check whether a provided twitter handle already exists online.",
  "repository": {
    "type": "git",
    "url": "https://github.com/benderham/twitter-validate"
  },
  "main": "lib/main.js",
  "scripts": {
    "test": "npm run build && npm run build:test && mocha test/test.js",
    "build": "lsc -o lib/ -c src/",
    "build:test": "lsc -c test/"
  },
  "keywords": [
    "twitter",
    "validate"
  ],
  "author": "Ben Derham",
  "license": "ISC",
  "devDependencies": {
    "chai": "^3.5.0",
    "livescript": "^1.5.0",
    "mocha": "^3.0.2"
  }
}
